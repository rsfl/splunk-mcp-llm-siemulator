# ollama:* and mcp:* sourcetypes are now owned by TA-ollama-releasev1 and TA-mcp-jsonrpc.
# Their props.conf files in apps/*/default/props.conf take precedence.
# Only project-specific overrides belong here.
