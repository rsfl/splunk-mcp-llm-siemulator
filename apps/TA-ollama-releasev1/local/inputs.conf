# Local override for Docker deployment
# Monitors Ollama server logs written to the shared ./logs volume

[monitor:///var/log/docker-apps/ollama.log]
disabled = 0
index = ollama
sourcetype = ollama:server
host = security-range-ollama
