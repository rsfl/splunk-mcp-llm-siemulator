# Local override for Docker deployment
# Monitors MCP server logs written to the shared ./logs volume

[monitor:///var/log/docker-apps/mcp.log]
disabled = 0
index = mcp
sourcetype = mcp:jsonrpc
host = security-range-ollama-mcp
